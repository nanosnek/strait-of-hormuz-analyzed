# Global Fishing Watch (gfw.py, main.py presence/events) -- the main source.
# Official client; needs a free API token, see .env.example.
gfw-api-python-client>=1.4

# MarineTraffic live map (livemap.py, main.py snapshot/watch).
# curl_cffi impersonates Chrome's TLS fingerprint, which Cloudflare requires;
# plain requests/urllib get a 403 there.
curl_cffi>=0.7

# MarineTraffic reports endpoint (data_get.py)
requests>=2.31

# Mapping (mapping.py, main.py map). pandas comes in with geopandas but
# mapping.py imports it directly, so it is listed.
geopandas>=1.0
matplotlib>=3.8
pandas>=2.0
